services:
  - type: web
    name: kids-drawing-wall-v2
    runtime: node
    plan: free
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: BASE_URL
        sync: false
      - key: SESSION_TTL_HOURS
        value: "12"
